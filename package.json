{
  "name": "traefik-github-auth",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "npx nodemon --legacy-watch src/main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild src/main.js --bundle --platform=node --outfile=dist/main.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-session": "^2.0.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "passport": "^0.5.3",
    "passport-github2": "^0.1.12"
  },
  "devDependencies": {
    "esbuild": "^0.16.10",
    "prettier": "^2.8.1"
  }
}
